<script>
  var allElements = new Backbone.Collection();
  allElements.reset(<%= @elements.to_json %>);
  var pageCollection = new Mock.page.Collection({
    url: 'pages/'
  });
  pageCollection.reset(<%= @pages.to_json %>);
</script>